server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri /index.html;  # needed for React routing
  }
  location /api {
    proxy_pass http://backend:5000;  # "backend" = docker-compose service name
  }
}
